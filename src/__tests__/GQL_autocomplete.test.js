/* @flow */
// import watch from '../utils/watch';
// import { GQL } from '../GQL';
// import path from 'path';

// jest.mock('../utils/watch');

// describe('autocomplete', () => {
//   let gql;

//   beforeEach(() => {
//     gql = new GQL({ cwd: path.resolve('src/__test-data__/') });
//     watch.__triggerChange();
//   });

//   // test works with query files

//   // test works with schema files

//   // return [] for files not matching config

test('works', () => expect(5).toEqual(5));
